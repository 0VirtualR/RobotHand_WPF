<Window x:Class="RobotHand_WPF_20290319.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RobotHand_WPF_20290319"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:ext="clr-namespace:RobotHand_WPF_20290319.Extensions"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"   Height="600"   Width="1000"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
        Style="{DynamicResource MaterialDesignWindow}">

    <materialDesign:DialogHost x:Name="DialogHost" Identifier="Root">
        <materialDesign:DrawerHost x:Name="drawerHost" IsRightDrawerOpen="{Binding ElementName=MenuToggleButton,Path=IsChecked}">
            <materialDesign:DrawerHost.RightDrawerContent>
                <DockPanel MinWidth="200" Margin="10,30">
                    <TextBlock Text="设置选择" FontSize="33" DockPanel.Dock="Top"/>
                    <ListBox x:Name="MenuBar" ItemContainerStyle="{StaticResource MyListBoxItemStyle}"
                             ItemsSource="{Binding MenuBars}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding NavigateCommand}"
   CommandParameter="{Binding SelectedItem,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=ListBox}}"/>
                                
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Background="Transparent" Orientation="Horizontal">
                                    <materialDesign:PackIcon Margin="15,0" Kind="{Binding Icon}"/>
                                    <TextBlock Margin="10,0" Text="{Binding Title}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </materialDesign:DrawerHost.RightDrawerContent>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"/>
                    <RowDefinition/>
                    <RowDefinition Height="60"/>
                </Grid.RowDefinitions>
                <materialDesign:Snackbar x:Name="Snackbar" Panel.ZIndex="1" MessageQueue="{materialDesign:MessageQueue}"/>
                <materialDesign:ColorZone x:Name="ColorZone" Padding="16"   Mode="PrimaryMid" >
                    <DockPanel>
                        <TextBlock Text="上位机注胶程序" FontSize="34" Margin="20,0"/>
                        <DockPanel LastChildFill="False">
                            <ToggleButton
         x:Name="MenuToggleButton"
         AutomationProperties.Name="HamburgerToggleButton"
         IsChecked="False"
         Style="{StaticResource MaterialDesignHamburgerToggleButton}" />

                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button   x:Name="btnMin"  Content="—"
       Style="{StaticResource MaterialDesignFlatMidBgButton}" />
                                <Button   x:Name="btnMax"  Content="☐"  
       Style="{StaticResource MaterialDesignFlatMidBgButton}" />
                                <Button   x:Name="btnClose"    Content="✕"
       Style="{StaticResource MaterialDesignFlatMidBgButton}" />
                            </StackPanel>
                        </DockPanel>
                    </DockPanel>
                </materialDesign:ColorZone>
                <ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static ext:PrismManager.MainViewRegionName }"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <TextBlock Text="提示信息1" FontSize="29" Margin="10"/>
                    <TextBlock Text="提示信息2" FontSize="29" Margin="10"/>
                    <TextBlock Text="提示信息3" FontSize="29" Margin="10"/>
                </StackPanel>

            </Grid>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</Window>
