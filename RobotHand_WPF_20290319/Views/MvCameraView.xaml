<UserControl x:Class="RobotHand_WPF_20290319.Views.MvCameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RobotHand_WPF_20290319.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="初始化" Command="{Binding InitializeCommand}" Margin="0,0,8,0"/>
            <Button Content="播放" Command="{Binding StartCommand}" Margin="0,0,8,0"/>
            <Button Content="停止" Command="{Binding StopCommand}" Margin="0,0,8,0"/>
            <Button Content="设置" Command="{Binding SettingCommand}" Margin="0,0,8,0"/>
            <Button Content="抓拍" Command="{Binding SnapCommand}" />
        </StackPanel>

        <Border Grid.Row="1" Margin="10" BorderBrush="Gray" BorderThickness="1">
            <Image Source="{Binding ImageSource}" Stretch="Uniform"/>
        </Border>

        <TextBlock Grid.Row="2" Margin="10" Text="{Binding StatusText}" />
    </Grid>
</UserControl>
